#include "holberton.h"

/**
 * main - entry point
 * Return: always 0 (success)
 */

void print_alphabet(void);

int main(void);
{
char x;

for (x = 'a'; x <= 'z'; x++)

_putchar(x);

_putchar('\n');
return (0);
}
